#include<bits/stdc++.h>
using namespace std;


class TreeNode{
    public:
    int data;
    vector<TreeNode*>
};




                    


int main();
/*
# **Tree নোডের children ভেক্টরে কী সংরক্ষণ হয়?**  

আপনার প্রশ্নটি ছিল:  

> **`root->children.push_back(child);`**  
> - এখানে `child` নোডের **ডেটা ও অ্যাড্রেস** `children` ভেক্টরে স্টোর হয়।  
> - `children` ভেক্টরের ভিতরের মান `root` নোডে সংরক্ষিত হয় এবং `root` ও `child` নোডের মধ্যে **কানেকশন** তৈরি করে।  

এই ৩টি বাক্য **কতটুকু সঠিক?**  

---

## **সঠিক ব্যাখ্যা (বাংলায়):**  

### **১. `root->children.push_back(child);` কী করে?**  
- `root` নোডের `children` ভেক্টরে `child` নোডের **মেমরি অ্যাড্রেস (পয়েন্টার)** যোগ করে।  
- এটি **প্যারেন্ট-চাইল্ড রিলেশনশিপ** তৈরি করে (`root` → `child`)।  

### **২. "`child` নোডের ডেটা ও অ্যাড্রেস `children` ভেক্টরে স্টোর হয়"**  
✅ **আংশিক সঠিক**, তবে **সংশোধন প্রয়োজন**:  
- `children` ভেক্টর **শুধু `child` নোডের অ্যাড্রেস (`TreeNode*`) স্টোর করে**, ডেটা (`child->data`) সরাসরি **স্টোর করে না**।  
- `child` নোডের ডেটা (`data`) **নোডের ভিতরেই** আলাদাভাবে সংরক্ষিত থাকে।  

### **৩. "`children` ভেক্টরের মান `root` নোডে সংরক্ষিত হয় এবং `root` ও `child` নোডের মধ্যে কানেকশন তৈরি করে"**  
✅ **সঠিক**, তবে আরও পরিষ্কারভাবে:  
- `children` ভেক্টর **`root` নোডের একটি প্রপার্টি** (এটি `root`-এর ভিতরেই থাকে)।  
- `push_back(child)` কল করার মাধ্যমে `root`-এর `children` লিস্টে `child`-এর **অ্যাড্রেস যোগ হয়**, ফলে `root` থেকে `child`-এ **একদিকীয় কানেকশন** তৈরি হয়।  

---

## **মেমরি ডায়াগ্রাম (বাংলায়):**  

```plaintext
root (data = 1)
│
├── children[0] → child1 (data = 2) [অ্যাড্রেস: 0x123ABC]
│   │
│   ├── children[0] → grandchild1 (data = 4)
│   └── children[1] → grandchild2 (data = 5)
│
└── children[1] → child2 (data = 3) [অ্যাড্রেস: 0x456DEF]
```

### **Key Points (গুরুত্বপূর্ণ বিষয়):**  
1. **`children` ভেক্টর শুধু পয়েন্টার স্টোর করে**, পুরো নোড নয়।  
2. **`root` জানতে পারে তার চাইল্ড কারা**, কিন্তু `child` জানতে পারে না কে তার প্যারেন্ট (একদিকীয় সম্পর্ক)।  
3. **`push_back(child)` শুধু অ্যাড্রেস যোগ করে**, ডেটা কপি করে না।  

---

## **সহজ উদাহরণ (C++ কোড সহ):**  

```cpp
TreeNode* root = new TreeNode(1);    // root নোড (data=1)
TreeNode* child = new TreeNode(2);  // child নোড (data=2)
root->children.push_back(child);    // root-এর children-এ child-এর অ্যাড্রেস যোগ
```

### **মেমরিতে কী ঘটে?**  
| ভেরিয়েবল | স্টোর করে          | বর্ণনা                     |
|----------|-------------------|----------------------------|
| `root`   | `TreeNode*`       | `root` নোডের অ্যাড্রেস (data=1)। |
| `child`  | `TreeNode*`       | `child` নোডের অ্যাড্রেস (data=2)। |
| `root->children` | `vector<TreeNode*>` | `[ &child ]` (অ্যাড্রেসের লিস্ট)। |

---

## **সচরাচর ভুল ধারণা (Misconceptions):**  
❌ **"`children` ভেক্টর `child` নোডের ডেটা স্টোর করে।"**  
✅ **সত্য:** এটি শুধু **অ্যাড্রেস** রাখে, ডেটা নয়।  

❌ **"`root` ও `child` নোডের মধ্যে দুইদিকীয় কানেকশন হয়।"**  
✅ **সত্য:** এটি **একদিকীয়** (শুধু `root` → `child`), `child` জানতে পারে না কে তার প্যারেন্ট।  

---

## **সারমর্ম (Conclusion):**  
1. **`push_back(child)`** → `root`-এর `children` লিস্টে `child`-এর **অ্যাড্রেস** যোগ করে।  
2. **`children` ভেক্টর** শুধু পয়েন্টার স্টোর করে, ডেটা নয়।  
3. এটি **ট্রি ডাটা স্ট্রাকচার** তৈরি করে (`root` → `child` → `grandchild`)।  

আরও ক্লিয়ার করতে চাইলে ডায়াগ্রাম বা কোড এক্সাম্পল দিতে পারি! 😊
*/